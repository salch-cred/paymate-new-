# Superseded by web/.env.example.
#
# The backend now lives entirely inside the Next.js app (web/src/app/api/**),
# so all runtime configuration is a single web/.env.local file. See
# web/.env.example for the current list of variables.
