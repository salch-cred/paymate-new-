name: Relayer Run (every 10 min)

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

concurrency:
  group: relayer-run
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Ping /api/relayer/run
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          set -euo pipefail
          code=$(curl -sS -o /tmp/relayer_body.txt -w "%{http_code}" \
            --max-time 280 \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            "https://www.paymateagent.xyz/api/relayer/run")
          echo "${code}" > /tmp/relayer_code.txt
          echo "HTTP ${code}"
          cat /tmp/relayer_body.txt
          [ "${code}" = "200" ]

      - name: Notify Discord on failure
        if: failure()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -n "${DISCORD_WEBHOOK_URL}" ]; then
            code=$(cat /tmp/relayer_code.txt 2>/dev/null || echo "?")
            body=$(cat /tmp/relayer_body.txt 2>/dev/null | head -c 1200)
            python3 - "$code" "$body" <<'PY'
import json, os, subprocess, sys
code, body = sys.argv[1], sys.argv[2]
msg = "🔴 PayMate relayer/run failed (HTTP {})\n```{}```".format(code, body)
subprocess.run(["curl", "-sS", "--max-time", "20", "-H", "Content-Type: application/json",
                "-d", json.dumps({"content": msg}), os.environ["DISCORD_WEBHOOK_URL"]])
PY
          fi
